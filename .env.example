# Ask Sika AI assistant (server-side only — set in Vercel, not VITE_*)
# Create key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: extra CORS origins for /api/ask (comma-separated, e.g. custom domain)
# ALLOWED_ORIGINS=https://sikatrack.example.com
